# JWT Authentication API 🚀

API simples de **autenticação com JWT** usando **Node.js** e **Express**.

Este projeto demonstra como implementar uma rota de login que autentica usuários através de **JSON Web Tokens (JWT)**, retornando um token válido que pode ser usado para proteger outras rotas da API.

---

## 📌 Funcionalidades

- ✔ Login de usuário com validação de credenciais  
- ✔ Geração de token JWT assinado com segredo  
- ✔ Estrutura básica de servidor Express  
- ✔ Suporte a JSON no corpo das requisições via `express.json()`
